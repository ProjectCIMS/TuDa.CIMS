# Normalize text files by default
* text=auto

# Ensure POSIX shell scripts are LF (fixes shebang issues)
*.sh text eol=lf

# Ensure Dockerfiles are LF
Dockerfile text eol=lf

# Windows-specific scripts may keep CRLF
*.ps1 text eol=crlf

