<MudDialog>
    <TitleContent>
        <MudItem>@Purchase.CreatedAt.ToString("dd.MM.yyyy")</MudItem>
        <MudItem>
            @if (Purchase.Invalidated)
            {
                <MudChip T="string" Color="Color.Error">Rechnung ist ungültig</MudChip>
            }
            @if (!Purchase.Invalidated)
            {
                <MudButton Variant="Variant.Filled" Color="Color.Error" OnClick="@(() => GoToInvalidation())">
                    Invalidieren
                </MudButton>
            }
        </MudItem>
    </TitleContent>
    <DialogContent>
        <CascadingValue Value="Purchase">
            <MudItem xs="6">
                <PurchaseInformationPurchaseEntryList/>
            </MudItem>
            <MudItem xs="6">
                <MudStack>
                    <MudItem>
                        <PurchaseInformationBuyerInformation/>
                    </MudItem>
                    <MudItem>
                        <PurchaseInformationStatistics/>
                    </MudItem>
                </MudStack>
            </MudItem>
        </CascadingValue>
    </DialogContent>
</MudDialog>
