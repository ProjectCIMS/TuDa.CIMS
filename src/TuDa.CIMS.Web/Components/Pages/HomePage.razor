@page "/"
@code {
    [Inject] private NavigationManager _navigationManager { get; set; } = null!;

    protected override void OnInitialized()
    {
        _navigationManager.NavigateTo("asset-list", replace: true);
    }
}
