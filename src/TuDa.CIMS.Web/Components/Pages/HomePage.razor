@page "/"
@code {
    [Inject] private NavigationManager _navigationManager { get; set; } = null!;

    protected override void OnInitialized()
    {
        // This will automatically navigate the user to the assetList if the homepage is entered
        _navigationManager.NavigateTo("shop", replace: true);
    }
}
