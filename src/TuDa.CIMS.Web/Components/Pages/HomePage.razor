@page "/"
@code {
    private readonly NavigationManager _navigationManager;

    public HomePage(NavigationManager navigationManager) => _navigationManager = navigationManager;

    protected override void OnInitialized()
    {
        // This will automatically navigate the user to the shop if the homepage is entered
        _navigationManager.NavigateTo("shop", replace: true);
    }
}
